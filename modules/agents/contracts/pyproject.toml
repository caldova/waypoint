[project]
name = "contracts"
version = "0.1.0"
description = "Waypoint Contracts — Teams-first contract intake and evidence autopilot stub."
requires-python = ">=3.11"
dependencies = [
    "castia[optimize]==0.7.7",
    "python-dotenv>=1.0",
]

[tool.uv]
package = false
prerelease = "allow"
environments = ["sys_platform == 'linux'", "sys_platform == 'win32'"]
